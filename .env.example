# Postgres connection string (e.g. a free Supabase project).
# Project Settings -> Database -> Connection string -> URI, swap in your password.
DATABASE_URL=

# Password for the site's built-in login gate. Leave empty to disable
# protection entirely (middleware treats a missing password as "no protection").
SITE_PASSWORD=
