# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env files — .env.example is NOT ignored: it's the template and must be committed
.env
.env*.local

# local Freebuff workspace: preview logs, run notes, and the worktrees
# themselves. Machine-local procedures (including how to read a credential)
# belong in here rather than in a committed document, so this must stay ignored.
/.freebuff/

# misc
.DS_Store
*.pem
npm-debug.log*
*.tsbuildinfo

# prisma
prisma/dev.db

dashboard-batch-*.patch
dashboard-compiled-3-to-7.patch
redesign-instructions.md